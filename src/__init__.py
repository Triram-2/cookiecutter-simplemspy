# src/__init__.py
"""
Корневой пакет приложения.

Этот файл (`__init__.py`) делает директорию `src` Python-пакетом.

В большинстве случаев этот файл может оставаться пустым или содержать
минимальное количество кода, например, для определения метаданных пакета
(таких как `__version__`), если это необходимо.

Если требуются какие-либо глобальные инициализации на уровне всего пакета `src`
(что бывает редко), они могут быть размещены здесь. Однако, обычно
инициализация специфичных компонентов происходит в их собственных модулях или
в точке входа приложения (например, в `src/main.py` или `src/core/config.py`).

Для данного шаблона микросервиса, основная задача этого файла - просто
обозначить `src` как пакет.
"""

# Пример определения версии пакета (опционально):
# __version__ = "0.1.0"

# Можно также использовать для удобного импорта ключевых компонентов,
# но это следует делать с осторожностью, чтобы не создавать циклических зависимостей
# и не перегружать пространство имен пакета.
# Например, если бы `settings` или `app` были нужны очень часто из `src`:
# from .core.config import settings
# from .api import app
#
# __all__ = ["settings", "app"]
# (Но для данного шаблона это не рекомендуется, лучше импортировать из конкретных подмодулей)
